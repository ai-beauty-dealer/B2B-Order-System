.DS_Store
backend/gas_deploy/projects.json

# 設計書・手順書はリポジトリに置かない。
# このリポジトリはPublicで、かつGitHub Pagesがルートを丸ごと配信するため、
# docs/ に置いたものは https://.../docs/ファイル名 で誰でも読めてしまう。
# （共通パスワードや社員セットアップ手順が公開されていた）
# ファイルは手元のdocs/にそのまま残っている。
docs/
manual/
CLAUDE-SECURITY-*/
backend/node_modules/
自動入力ツール/
__pycache__/
*.pyc

# バックエンド・生成スクリプトは公開Pagesから配信しない
# （GAS本体・デプロイ台帳ツール・シード処理は攻撃者に内部構造を晒すため）
backend/
# 納品書集約ツールのGASソース。backend/ と同じ性質（集約コード変換の業務ロジック）
gas-tools/
# backend/ の実ファイルを読み込むテストと、公開除外したdocs/を参照するUT設定。
# 追跡すると「読み込み先が無い壊れたテスト」になるうえ、
# 外したはずのGAS内部（関数名・スクリプトプロパティ名）が本文から漏れる。
tests/test_line_user_id_capture.mjs
tests/test_shipping_import.mjs
ut.config.yaml
gen_history_favs.py
app.backup.js
style.backup.css
